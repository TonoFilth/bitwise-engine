include(${BW_MACRO_DIR}/sample.cmake)

sample("module_core" GROUP "engine")
sample("module_math" GROUP "engine")
sample("assert"      GROUP "engine")
sample("bits"        GROUP "engine")
sample("callstack"   GROUP "engine")
sample("cstring"     GROUP "engine")
sample("logs"        GROUP "engine")
sample("pointer"     GROUP "engine")